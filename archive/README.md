# Archived PySide6 desktop UI

This tree was moved out of `src/cuepoint/ui/` during Phase 10 cutover.

**Shipping desktop app:** [`apps/desktop-electron`](../apps/desktop-electron)

**Shared non-Qt helpers:** [`src/cuepoint/compat/`](../src/cuepoint/compat/)

This archive is reference-only. It is not on `PYTHONPATH` and is not imported by
the Electron engine or `src/gui_app.py`.

Related archived tests:

- `archive/legacy-pyside6-ui-tests/`
- `archive/legacy-pyside6-unit-ui-tests/`
