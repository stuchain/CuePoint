# Development dependencies for CuePoint
# Install with: pip install -r requirements-dev.txt

# Testing framework
pytest==9.0.2
hypothesis==6.98.0  # Property-based tests (Design 3.15)
pytest-cov==7.0.0
pytest-mock==3.15.1
pytest-asyncio==1.3.0
pytest-timeout==2.4.0
pytest-xdist==3.8.0  # For parallel test execution
pytest-benchmark==5.2.3  # For performance benchmarks (Step 7.3)

# Coverage reporting
coverage==7.13.1

# Type stubs for mypy
types-requests
types-psutil
types-PyYAML
types-tqdm

# Code quality
# Pinned, not >=. CI enforces `ruff check src/` and `ruff format --check src/`
# as hard gates, and this project has no [tool.ruff] config, so it runs on
# ruff's defaults. Those defaults change between releases: 0.16 expanded them
# and reports thousands of violations against this codebase, which would fail
# the gate on an unrelated PR with no code change. Upgrade deliberately, with
# the resulting lint fixes, rather than by surprise.
ruff==0.14.0
black==26.3.1
pylint==4.0.4
mypy==1.19.1
isort==7.0.0
flake8==7.3.0
pre-commit==4.5.1
radon==6.0.1  # For complexity metrics (5.7.18)

# Build tools
pyinstaller==6.17.0  # For building executables (Python 3.13-compatible)
pip-tools  # For generate_requirements_hashes.py (Design 2.8, pip --require-hashes)

# Optional legacy Qt UI tests: pip install -r requirements-qt.txt
