/**
 * Preload bridge stub (Phase 4 / ADR-004).
 * Expose minimal typed surface via contextBridge when Electron deps are added.
 */
// Placeholder — implement with contextBridge.exposeInMainWorld in Phase 4.
