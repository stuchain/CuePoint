interface:
  display_name: "CuePoint Release"
  short_description: "Drive a full CuePoint release: docs, changelogs, green CI, tag, verify"
  default_prompt: "Use $cuepoint-release to take this CuePoint release through the run sheet - docs and changelog sync, green release CI, version coupling and local gates - without tagging or publishing until authorized."
