# Optional Qt compatibility dependencies

# The shipped desktop app is Electron. Install this file only when running the
# remaining Qt-dependent compatibility and update-system tests.

PySide6==6.10.1
pytest-qt==4.5.0
